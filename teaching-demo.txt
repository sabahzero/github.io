<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sabah Ul-Hasan, Ph.D. — Teaching Demo</title>

    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:opsz,wght@14..32,300;14..32,400;14..32,500;14..32,600;14..32,700&family=Playfair+Display:ital,wght@0,600;0,700;1,600&display=swap" rel="stylesheet" />

    <style>
        /* ---------- CSS CUSTOM PROPERTIES ---------- */
        :root {
            --bg-primary: #f6f9fc;
            --bg-secondary: #ffffff;
            --bg-card: rgba(255, 255, 255, 0.72);
            --bg-card-hover: rgba(255, 255, 255, 0.92);
            --bg-nav: rgba(26, 26, 46, 0.92);
            --bg-hero-start: #0b1a33;
            --bg-hero-end: #1a2a4a;
            --text-primary: #0b1a33;
            --text-secondary: #2d3a5a;
            --text-muted: #6b7a9a;
            --text-light: #f0f4fc;
            --accent: #4f9cf7;
            --accent-glow: rgba(79, 156, 247, 0.25);
            --border-subtle: rgba(0, 0, 0, 0.06);
            --shadow-sm: 0 2px 12px rgba(0, 0, 0, 0.04);
            --shadow-md: 0 8px 32px rgba(0, 0, 0, 0.08);
            --shadow-lg: 0 16px 56px rgba(0, 0, 0, 0.12);
            --radius: 20px;
            --radius-sm: 12px;
            --transition: 0.35s cubic-bezier(0.22, 1, 0.36, 1);
            --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            --font-serif: 'Playfair Display', Georgia, serif;
        }

        [data-theme="dark"] {
            --bg-primary: #0d1424;
            --bg-secondary: #161f35;
            --bg-card: rgba(22, 31, 53, 0.78);
            --bg-card-hover: rgba(30, 44, 72, 0.92);
            --bg-nav: rgba(13, 20, 36, 0.95);
            --bg-hero-start: #070e1a;
            --bg-hero-end: #0f1f3a;
            --text-primary: #eef3fc;
            --text-secondary: #c8d6e8;
            --text-muted: #8a9bb8;
            --text-light: #eef3fc;
            --border-subtle: rgba(255, 255, 255, 0.06);
            --shadow-sm: 0 2px 12px rgba(0, 0, 0, 0.25);
            --shadow-md: 0 8px 32px rgba(0, 0, 0, 0.35);
            --shadow-lg: 0 16px 56px rgba(0, 0, 0, 0.45);
        }

        /* ---------- RESET & BASE ---------- */
        *,
        *::before,
        *::after {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font-sans);
            background: var(--bg-primary);
            color: var(--text-primary);
            line-height: 1.7;
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            transition: background 0.4s ease, color 0.4s ease;
            -webkit-font-smoothing: antialiased;
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        /* ---------- SCROLLBAR ---------- */
        ::-webkit-scrollbar {
            width: 8px;
        }
        ::-webkit-scrollbar-track {
            background: var(--bg-primary);
        }
        ::-webkit-scrollbar-thumb {
            background: var(--accent);
            border-radius: 8px;
        }

        /* ---------- NAVIGATION ---------- */
        nav {
            position: sticky;
            top: 0;
            z-index: 1000;
            background: var(--bg-nav);
            backdrop-filter: blur(16px) saturate(180%);
            -webkit-backdrop-filter: blur(16px) saturate(180%);
            border-bottom: 1px solid var(--border-subtle);
            transition: background 0.4s ease, border-color 0.4s ease;
        }

        .nav-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 12px 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px 20px;
        }

        .logo {
            font-family: var(--font-serif);
            font-weight: 700;
            font-size: 1.3rem;
            color: #fff;
            letter-spacing: -0.3px;
        }
        .logo span {
            color: var(--accent);
        }

        .nav-right {
            display: flex;
            align-items: center;
            gap: 6px;
            flex-wrap: wrap;
        }

        .nav-links {
            display: flex;
            gap: 4px;
            flex-wrap: wrap;
        }

        .nav-links a {
            color: rgba(255, 255, 255, 0.7);
            padding: 6px 16px;
            border-radius: 40px;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            letter-spacing: 0.2px;
        }
        .nav-links a:hover {
            color: #fff;
            background: rgba(255, 255, 255, 0.10);
        }
        .nav-links a.active {
            color: #fff;
            background: var(--accent);
            box-shadow: 0 0 20px var(--accent-glow);
        }

        .theme-toggle {
            background: rgba(255, 255, 255, 0.08);
            border: none;
            border-radius: 40px;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            font-size: 1.2rem;
            color: #fff;
            transition: background 0.3s ease, transform 0.3s ease;
            margin-left: 4px;
            flex-shrink: 0;
        }
        .theme-toggle:hover {
            background: rgba(255, 255, 255, 0.18);
            transform: rotate(12deg);
        }

        /* ---------- HERO ---------- */
        .hero {
            position: relative;
            background: linear-gradient(145deg, var(--bg-hero-start), var(--bg-hero-end));
            padding: 70px 24px 60px;
            text-align: center;
            overflow: hidden;
            isolation: isolate;
            transition: background 0.5s ease;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -40%;
            right: -20%;
            width: 600px;
            height: 600px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(79, 156, 247, 0.12), transparent 70%);
            pointer-events: none;
            z-index: 0;
        }
        .hero::after {
            content: '';
            position: absolute;
            bottom: -30%;
            left: -10%;
            width: 400px;
            height: 400px;
            border-radius: 50%;
            background: radial-gradient(circle, rgba(79, 156, 247, 0.08), transparent 70%);
            pointer-events: none;
            z-index: 0;
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 820px;
            margin: 0 auto;
        }

        .hero h1 {
            font-family: var(--font-serif);
            font-size: 3rem;
            font-weight: 700;
            color: #fff;
            line-height: 1.15;
            margin-bottom: 6px;
            letter-spacing: -0.5px;
        }
        .hero h1 .highlight {
            color: var(--accent);
            background: linear-gradient(135deg, #4f9cf7, #7bb3ff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .hero .subtitle {
            font-size: 1.3rem;
            color: rgba(255, 255, 255, 0.8);
            font-weight: 400;
            margin-bottom: 4px;
        }

        .hero-divider {
            width: 60px;
            height: 3px;
            background: var(--accent);
            border-radius: 4px;
            margin: 16px auto 18px;
            opacity: 0.6;
        }

        .float-shape {
            position: absolute;
            border-radius: 50%;
            pointer-events: none;
            z-index: 0;
            opacity: 0.30;
        }
        .float-shape-1 {
            width: 120px;
            height: 120px;
            top: 10%;
            left: 5%;
            background: radial-gradient(circle, rgba(79, 156, 247, 0.20), transparent 70%);
            animation: float 12s ease-in-out infinite alternate;
        }
        .float-shape-2 {
            width: 80px;
            height: 80px;
            bottom: 15%;
            right: 8%;
            background: radial-gradient(circle, rgba(79, 156, 247, 0.15), transparent 70%);
            animation: float 16s ease-in-out infinite alternate-reverse;
        }

        @keyframes float {
            0% {
                transform: translate(0, 0) scale(1);
            }
            100% {
                transform: translate(20px, -30px) scale(1.1);
            }
        }

        /* ---------- MAIN CONTENT ---------- */
        .main-content {
            flex: 1;
            max-width: 1100px;
            margin: -30px auto 50px;
            padding: 0 24px;
            width: 100%;
            position: relative;
            z-index: 2;
        }

        .section-header {
            text-align: center;
            margin-bottom: 32px;
            padding-top: 40px;
        }
        .section-header h2 {
            font-family: var(--font-serif);
            font-size: 2rem;
            font-weight: 600;
            color: var(--text-primary);
            letter-spacing: -0.3px;
        }
        .section-header p {
            color: var(--text-muted);
            font-size: 1rem;
            margin-top: 4px;
        }

        .demo-container {
            background: var(--bg-card);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
            border-radius: var(--radius);
            padding: 40px;
            box-shadow: var(--shadow-sm);
            border: 1px solid var(--border-subtle);
            transition: all var(--transition);
        }
        .demo-container:hover {
            box-shadow: var(--shadow-md);
            border-color: rgba(79, 156, 247, 0.15);
        }

        .demo-container h2 {
            font-family: var(--font-serif);
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--text-primary);
            margin-top: 2rem;
            margin-bottom: 0.8rem;
            letter-spacing: -0.2px;
            border-bottom: 2px solid var(--accent);
            padding-bottom: 0.3rem;
            display: inline-block;
        }
        .demo-container h2:first-of-type {
            margin-top: 0;
        }

        .demo-container p {
            color: var(--text-secondary);
            margin-bottom: 0.8rem;
        }

        .demo-container ul {
            margin: 0.4rem 0 1rem 1.6rem;
            color: var(--text-secondary);
        }

        .demo-container li {
            margin-bottom: 0.3rem;
        }

        .video-container {
            position: relative;
            padding-bottom: 56.25%;
            height: 0;
            overflow: hidden;
            border-radius: var(--radius-sm);
            margin: 1.5rem 0;
            background: #000;
        }
        .video-container iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
        }

        .video-placeholder {
            background: var(--bg-secondary);
            border-radius: var(--radius-sm);
            padding: 40px 20px;
            text-align: center;
            border: 2px dashed var(--border-subtle);
            margin: 1.5rem 0;
            transition: background 0.4s ease, border-color 0.4s ease;
        }
        .video-placeholder .icon {
            font-size: 3rem;
            display: block;
            margin-bottom: 12px;
        }
        .video-placeholder p {
            color: var(--text-muted);
            margin: 0;
            font-size: 0.95rem;
        }
        .video-placeholder code {
            background: var(--bg-primary);
            padding: 2px 8px;
            border-radius: 4px;
            font-size: 0.85rem;
            color: var(--accent);
        }

        .back-link {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-top: 2rem;
            color: var(--accent);
            font-weight: 500;
            font-size: 0.95rem;
            transition: color 0.3s ease, transform 0.3s ease;
            padding: 6px 16px;
            border-radius: 40px;
            background: rgba(79, 156, 247, 0.06);
            border: 1px solid rgba(79, 156, 247, 0.10);
        }
        .back-link:hover {
            color: #7bb3ff;
            transform: translateX(-4px);
            background: rgba(79, 156, 247, 0.12);
        }

        .badge-list {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
            margin: 0.5rem 0 1rem 0;
        }
        .badge-list .badge {
            background: rgba(79, 156, 247, 0.08);
            border: 1px solid rgba(79, 156, 247, 0.12);
            border-radius: 30px;
            padding: 4px 14px;
            font-size: 0.75rem;
            font-weight: 600;
            color: var(--accent);
            letter-spacing: 0.3px;
        }

        /* ---------- FOOTER ---------- */
        .footer {
            background: var(--bg-nav);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            color: rgba(255, 255, 255, 0.5);
            text-align: center;
            padding: 28px 24px;
            font-size: 0.9rem;
            border-top: 1px solid var(--border-subtle);
            transition: background 0.4s ease, border-color 0.4s ease;
            margin-top: auto;
        }
        .footer-inner {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 12px 20px;
        }
        .footer a {
            color: var(--accent);
            font-weight: 500;
            transition: color 0.3s ease;
        }
        .footer a:hover {
            color: #7bb3ff;
        }
        .footer-social {
            display: flex;
            gap: 16px;
        }
        .footer-social a {
            color: rgba(255, 255, 255, 0.4);
            font-size: 1.1rem;
            transition: color 0.3s ease, transform 0.3s ease;
            display: inline-flex;
        }
        .footer-social a:hover {
            color: #fff;
            transform: translateY(-2px);
        }

        /* ---------- RESPONSIVE ---------- */
        @media (max-width: 768px) {
            .nav-container {
                flex-direction: column;
                gap: 8px;
                padding: 10px 16px;
            }
            .nav-links {
                justify-content: center;
            }
            .nav-links a {
                font-size: 0.8rem;
                padding: 5px 12px;
            }
            .theme-toggle {
                width: 36px;
                height: 36px;
                font-size: 1rem;
            }
            .hero h1 {
                font-size: 2.3rem;
            }
            .hero .subtitle {
                font-size: 1.1rem;
            }
            .hero {
                padding: 50px 16px 40px;
            }
            .demo-container {
                padding: 28px 20px;
            }
            .section-header h2 {
                font-size: 1.6rem;
            }
            .section-header {
                padding-top: 30px;
            }
            .footer-inner {
                flex-direction: column;
                text-align: center;
            }
        }

        @media (max-width: 480px) {
            .hero h1 {
                font-size: 1.9rem;
            }
            .nav-links a {
                font-size: 0.75rem;
                padding: 4px 10px;
            }
            .logo {
                font-size: 1.1rem;
            }
            .demo-container {
                padding: 20px 16px;
            }
            .section-header {
                padding-top: 20px;
            }
        }
    </style>
</head>
<body>

    <!-- ========== NAVIGATION ========== -->
    <nav role="navigation" aria-label="Main navigation">
        <div class="nav-container">
            <div class="logo">Sabah<span>Ul-Hasan</span></div>
            <div class="nav-right">
                <div class="nav-links">
                    <a href="/">Home</a>
                    <a href="/lesson-plan.html">Lesson Plan</a>
                    <a href="/teaching-demo.html" class="active">Demo</a>
                    <a href="/cv.html">CV</a>
                    <a href="/teaching-philosophy.html">Philosophy</a>
                </div>
                <button class="theme-toggle" id="themeToggle" aria-label="Toggle dark mode">
                    <span id="themeIcon">🌙</span>
                </button>
            </div>
        </div>
    </nav>

    <!-- ========== HERO ========== -->
    <header class="hero" role="banner">
        <div class="float-shape float-shape-1"></div>
        <div class="float-shape float-shape-2"></div>
        <div class="hero-content">
            <h1>Teaching <span class="highlight">Demo</span></h1>
            <p class="subtitle">Evidence-Based Practice in TESOL</p>
            <div class="hero-divider"></div>
        </div>
    </header>

    <!-- ========== MAIN CONTENT ========== -->
    <main class="main-content">

        <div class="section-header">
            <h2>3–5 Minute Teaching Video</h2>
            <p>Demonstrating TESOL-aligned teaching practices</p>
        </div>

        <div class="demo-container">

            <h2>Practices Demonstrated</h2>
            <p>This teaching demo showcases the following evidence-based practices aligned with TESOL philosophy:</p>
            <div class="badge-list">
                <span class="badge">Clear Objectives</span>
                <span class="badge">Scaffolding</span>
                <span class="badge">Comprehensible Input</span>
                <span class="badge">Student Interaction</span>
                <span class="badge">Positive Reinforcement</span>
                <span class="badge">Integration of Skills</span>
            </div>

            <h2>Teaching Video</h2>

            <!-- REPLACE THIS SECTION WITH YOUR YOUTUBE VIDEO -->
            <!--
            <div class="video-container">
                <iframe
                    src="https://www.youtube.com/embed/YOUR_VIDEO_ID"
                    frameborder="0"
                    allowfullscreen>
                </iframe>
            </div>
            -->

            <!-- PLACEHOLDER: Replace with actual video -->
            <div class="video-placeholder">
                <span class="icon">🎬</span>
                <p><strong>Video placeholder</strong></p>
                <p>Replace this section with your YouTube embed by uncommenting the video-container div above and replacing <code>YOUR_VIDEO_ID</code> with your actual YouTube video ID.</p>
            </div>

            <p style="font-size:0.9rem; color:var(--text-muted); margin-top:0.5rem;">
                <em>To add your video:</em> Upload to YouTube (unlisted), then replace <code>YOUR_VIDEO_ID</code> in the iframe <code>src</code> attribute.
            </p>

            <a href="/" class="back-link">← Back to Home</a>

        </div>

    </main>

    <!-- ========== FOOTER ========== -->
    <footer class="footer" role="contentinfo">
        <div class="footer-inner">
            <span>&copy; 2026 <a href="/">Sabah Ul-Hasan</a></span>
            <div class="footer-social">
                <a href="https://www.linkedin.com/in/sabahulhasan/" aria-label="LinkedIn">🔗</a>
                <a href="https://github.com/sabahzero" aria-label="GitHub">🐙</a>
                <a href="mailto:bysabahulhasan@gmail.com" aria-label="Email">✉️</a>
            </div>
        </div>
    </footer>

    <!-- ========== THEME TOGGLE SCRIPT ========== -->
    <script>
        (function() {
            const toggle = document.getElementById('themeToggle');
            const icon = document.getElementById('themeIcon');
            const root = document.documentElement;
            const stored = localStorage.getItem('theme');
            const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
            let currentTheme = stored || (prefersDark ? 'dark' : 'light');

            function setTheme(theme) {
                if (theme === 'dark') {
                    root.setAttribute('data-theme', 'dark');
                    icon.textContent = '☀️';
                } else {
                    root.removeAttribute('data-theme');
                    icon.textContent = '🌙';
                }
                currentTheme = theme;
                localStorage.setItem('theme', theme);
            }

            setTheme(currentTheme);

            toggle.addEventListener('click', () => {
                const next = currentTheme === 'light' ? 'dark' : 'light';
                setTheme(next);
            });

            window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
                if (!localStorage.getItem('theme')) {
                    setTheme(e.matches ? 'dark' : 'light');
                }
            });
        })();
    </script>

</body>
</html>